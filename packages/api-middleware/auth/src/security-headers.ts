/**
 * @deprecated Security headers are not part of the current auth-middleware spec.
 *
 * This file is intentionally empty. Use Hono's built-in `secureHeaders()` middleware
 * from `hono/secure-headers` instead, which is maintained and edge-compatible.
 *
 * @see https://hono.dev/docs/middleware/builtin/secure-headers
 */

export {};
